# 這個檔案只是「格式範例」，不會被程式讀取。
#
# 本機測試 Secrets 功能：複製這個檔案，改名為 secrets.toml（去掉 .example），填入真實帳密。
# secrets.toml 已被 .gitignore 排除，不會被上傳到 GitHub。
#
# 部署到 Streamlit Cloud 時：不用建立這個檔案，
# 而是到 app 的 Settings → Secrets 頁面，把下面的內容貼進去即可（線上直接設定，不經過 GitHub）。

MAIL_LOGIN_USER = "B12345678"
MAIL_PASSWORD = "你的密碼或應用程式密碼"
